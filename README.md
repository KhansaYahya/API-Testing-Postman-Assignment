Instructions
1. Import `Assignment4-API-Testing.postman_collection.json` in Postman.  
2. Import `Assignment4-Env.postman_environment.json` as the environment.  
3. Select the environment in the top-right corner.  
4. Run the requests in order: **POST → GET → PUT → PATCH → DELETE**.  
5. All tests and Chai assertions are included.

